### ۱. مسیر دسترسی (Navigation)
- **نقطه شروع:** صفحه اصلی سامانه
- **آدرس URL:** `/`
- **مسیر بردکرامب:** صفحه اصلی
- **راهنمای گام‌به‌گام:**
  ۱. به صفحه اصلی سامانه بروید.
  ۲. در پیج خانگی، به بخش مربوط به ثبت نام (کامپوننت `RegisterModal`) مراجعه کنید.
  ۳. مدال ثبت نام باز می‌شود و شما می‌توانید اطلاعات خود را وارد کنید.

---

### ۲. اطلاعات لازم و فیلدهای فرم (Form Fields)
فهرست فیلدهای موجود در فرم ثبت نام:

- **فیلد ورودی (text):**
  - **نوع:** ورودی متنی
  - **وضعیت:** فیلد اختیاری
  - **توضیح:** برای وارد کردن اطلاعات شخصی یا شناسه کاربری استفاده می‌شود.

> ⚠️ توجه: بر اساس داده‌های استخراج‌شده، فقط یک فیلد متنی در فرم شناسایی شده است. ممکن است فیلدهای دیگر در بخش‌های دیگر فرم یا در مراحل بعدی ظاهر شوند.

---

### ۳. دکمه اقدام نهایی (Action)
- **نام دکمه:** `SubmitATicket` (ثبت/ارسال درخواست)
- **وضعیت دکمه:** پس از تکمیل اطلاعات و پر شدن فیلدهای لازم، دکمه فعال می‌گردد. در غیر این صورت، دکمه در وضعیت غیرفعال (Disabled) قرار دارد.
- **رفتار سیستم پس از کلیک:**
  ۱. با کلیک روی دکمه، اعتبارسنجی داده‌ها بررسی می‌شود.
  ۲. در صورت موفقیت‌آمیز بودن اعتبارسنجی، فرم به سامانه ارسال (ثبت نهایی) می‌گردد.
  ۳. سیستم پیام تأییدین نمایش می‌دهد و فرم به مرحله بعدی هدایت می‌شود.
- **توضیح بصری دکمه:** دکمه دارای افکت‌های تعاملی می‌باشد؛ هنگام هاور (MouseEnter) رنگ پس‌زمینه دکمه تغییر می‌کند و هنگام خروج (MouseLeave) به حالت شفاف بازمی‌گردد."
